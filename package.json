{
  "name": "playwright-todomvc-pom",
  "version": "1.0.0",
  "description": "Playwright automation tests for TodoMVC with Page Object Model structure",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
